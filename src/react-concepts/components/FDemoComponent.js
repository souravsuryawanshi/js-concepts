const FDemoComponent = (props) => {
    return (
        <div>
            Hello World! {props.name}
        </div>
    )
}

export default FDemoComponent;


/*

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<h1>Hello World!</h1>);

*/